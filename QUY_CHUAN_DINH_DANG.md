# QUY CHUẨN ĐỊNH DẠNG VĂN BẢN ĐÃ THỐNG NHẤT (CHUẨN HÓA CỐ ĐỊNH)

Áp dụng cho toàn bộ văn bản trong dự án (Tờ trình, Báo cáo thẩm định, Quyết định, Thư mời, v.v.):

---

## I. BẢNG CỠ CHỮ CỐ ĐỊNH CHO TỪNG THÀNH PHẦN

### 1. Nhóm CỐ ĐỊNH 13.0 pt:
* **Mục 1 (Cơ quan chủ quản cấp trên):** `13.0 pt` (In hoa đứng, không đậm).
* **Mục 2 (Tên cơ quan ban hành):** `13.0 pt` (In hoa đứng, **in đậm**).
* **Mục 3 (Quốc hiệu):** `13.0 pt` (`CỘNG HÒA XÃ HỘI CHỦ NGHĨA VIỆT NAM`, in hoa đứng, **in đậm**, Condensed 0.8 pt).
* **Mục 4 (Tiêu ngữ):** `13.0 pt` (`Độc lập - Tự do - Hạnh phúc`, in thường đứng, **in đậm**).
* **Mục 5 (Số và ký hiệu văn bản):** `13.0 pt` (In thường đứng, ví dụ `Số: 14/TTr-BQL`).
* **Mục 6 (Địa danh, ngày tháng năm):** `13.0 pt` (In thường, *in nghiêng*, ví dụ `Hà Nội, ngày ... tháng ... năm ...`).
* **Mục 14 (Số trang văn bản):** `13.0 pt` (Căn giữa Header lề trên, bắt đầu hiển thị từ trang thứ 2).

### 2. Nhóm CỐ ĐỊNH 14.0 pt:
* **Mục 7 (Tên loại văn bản):** `14.0 pt` (TỜ TRÌNH, QUYẾT ĐỊNH, BÁO CÁO... in hoa đứng, **in đậm**).
* **Mục 8 (Trích yếu nội dung văn bản):** `14.0 pt` (In thường đứng, **in đậm**).
* **Mục 9 (Dòng Kính gửi):** `14.0 pt` (In thường đứng, kết thúc có dấu chấm `.`).
* **Mục 10 (Nội dung văn bản chính):** `14.0 pt` (In thường đứng, căn đều 2 bên Justify).
* **Mục 11 (Chức vụ người ký):** `14.0 pt` (In hoa đứng, **in đậm**, căn giữa khung ký).
* **Mục 12 (Họ và tên người ký):** `14.0 pt` (In thường/hoa, đứng, **in đậm**, căn giữa khung ký).

### 3. Nhóm Khung Nơi nhận & Khác:
* **Tiêu đề `Nơi nhận:`:** `12.0 pt` (*in nghiêng đậm*).
* **Nội dung các nơi nhận:** `11.0 pt` (In thường đứng).

---

## II. CĂN LỀ, THỤT LỀ & GIÃN DÒNG

1. **Khổ giấy & Căn lề chuẩn A4:**
   * Lề trên (Top): `20 mm` - `25 mm`.
   * Lề dưới (Bottom): `20 mm` - `25 mm`.
   * Lề trái (Left): `30 mm` - `35 mm` (đóng gáy hồ sơ).
   * Lề phải (Right): `15 mm` - `20 mm`.

2. **Căn lề, Thụt lề & Giãn dòng:**
   * Căn lề: Căn đều 2 bên (**Justify**).
   * Thụt lề đầu dòng (**First line indent**): Chuẩn **`36.0 pt` (1.27 cm / 0.5 inch)** cho tất cả các đoạn văn, căn cứ pháp lý, gạch đầu dòng `-`, dấu cộng `+`, các đầu mục số.
   * Giãn dòng: **Single line (1.0)**, không tạo khoảng cách đoạn (`space_before = 0`, `space_after = 0`).

3. **Quy tắc in đậm / in nghiêng:**
   * Các mục con (ví dụ: `1. Tên dự án:`, `2. Cấp quyết định đầu tư:`): **In đậm phần nhãn tiêu đề**, phần nội dung sau dấu `:` là chữ thường.
   * Phần kết luận & kiến nghị: **In đậm chữ số đầu dòng** (`1.`, `2.`), phần số tiền bằng chữ trong ngoặc đơn được **in nghiêng**.
   * Ký hiệu kết thúc văn bản: `./.` ở cuối nội dung.

4. **Bảng biểu (Table):**
   * Header: Căn giữa, in đậm, size `14.0 pt`.
   * Cột STT: Căn giữa, size `14.0 pt`.
   * Cột Nội dung/Tên chi phí: Căn đều 2 bên (Justify), size `14.0 pt`.
   * Cột Số tiền: Căn phải (Right), size `14.0 pt`.
   * Dòng tổng cộng: Cột tên căn giữa in đậm, cột tiền căn phải in đậm.
